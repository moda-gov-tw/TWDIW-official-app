<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 取消軟體與硬體限制 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"
        tools:node="replace" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true"
        tools:node="replace" />

    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false"
        tools:node="replace" />

    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data
                android:host="*"
                android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:name="tw.gov.moda.digitalwallet.AppApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Digitalwallet">

        <activity
            android:name="tw.gov.moda.digitalwallet.ui.link.AppLinkActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:permission="">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="${APPLINK_HOST}"
                    android:pathPrefix="/api/moda/qrcode"
                    android:scheme="https" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="modadigitalwallet" />
            </intent-filter>
        </activity>
        <activity
            android:name="tw.gov.moda.digitalwallet.ui.main.MainActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:permission=""
            android:windowSoftInputMode="adjustPan">
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>